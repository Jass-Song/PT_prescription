# sw-db-architect 월간 작업 로그 — 2026-05

> 작업할 때마다 append 방식으로 기록합니다. 삭제 금지.

---

## 2026-05-01 | todo-20260501-embedding-reembed Voyage API retry 개선 + 전체 재임베딩

- **지시 받은 내용**: embed-techniques.js에 retry 로직 추가 후 전체 재임베딩 실행
- **수행한 작업**: withRetry 함수(지수 백오프, 최대 5회 재시도) 추가, --force-all 플래그 추가, --only-new && !forceAll 조건 수정, getEmbeddings 호출부를 withRetry로 래핑, 전체 357개 재임베딩 실행
- **결과물**: `saas/scripts/embed-techniques.js` (수정), `technique_embeddings` 테이블 357개 갱신
- **이슈/메모**: --only-new 실행 시 357개 모두 이미 임베딩 완료 확인(0개 누락). --force-all로 전체 재임베딩 실행 (119배치 × 21초 ≈ 42분 소요)

---

## 2026-05-03 | C-3 + C-4 — ratings 효과 평가 컬럼 + recommendation_weight 트리거

- **지시 받은 내용**: C-3 부족분 (ratings 에 outcome / indication_accuracy 컬럼 추가) + C-4 (자동 갱신 트리거 등록 + 검증 스크립트). C-1 패스, C-2 자격증 제외.
- **수행한 작업**:
  1. `feat/auth-oauth-2026-05-03` 브랜치의 schema.sql 분석 → 트리거 함수·트리거 정의 위치(441~485) + ratings/technique_stats 풀 DDL 확인
  2. **Migration 049** 작성: `rating_outcome` ENUM (DO block 멱등) + `ratings.outcome` / `indication_accuracy` / `was_ai_recommended` 컬럼 추가 (ADD COLUMN IF NOT EXISTS, CHECK 제약)
  3. **Migration 050** 작성: `technique_stats` 누락 컬럼 12개 ADD + `fn_refresh_technique_stats()` 함수 (CREATE OR REPLACE) + `trg_refresh_stats_on_rating` 트리거 (DROP IF EXISTS + CREATE) + 기존 ratings 백필 INSERT…ON CONFLICT
  4. **검증 스크립트** 작성 `verify-recommendation-weight-trigger.sql`: Section 1~4 (스키마 확인 / 함수·트리거 등록 확인 / BEGIN…ROLLBACK end-to-end 5★+excellent / 1★+adverse / 잔여 데이터 확인)
- **결과물**:
  - `pt-prescription/saas/migrations/049-ratings-effect-columns.sql`
  - `pt-prescription/saas/migrations/050-recommendation-weight-trigger.sql`
  - `pt-prescription/saas/scripts/verify-recommendation-weight-trigger.sql`
- **이슈/메모**:
  - 백필 초안에서 `UPDATE ... LIMIT 1` 비표준 패턴 사용 → 직접 INSERT…ON CONFLICT GROUP BY technique_id 로 교체
  - Migration 050 의 `ALTER TABLE technique_stats ADD COLUMN IF NOT EXISTS` 12개 — production 에 일부만 있을 가능성 대비 안전 처리
  - 가중치 공식: 별점 20% + 적응증 정확도 30% + 30일 활성 20% + 효과 분류(excellent+good 비율) 30%, 범위 0.0000~1.0000
  - 대표님 액션: Supabase SQL 에디터에서 049 → 050 → verify 순으로 실행 필요

---

## 2026-05-03 | C-4 hotfix — 050b 트리거 함수 보안·정확성 수정

- **지시 받은 내용**: PR #31 머지 직후 정적 QA 리뷰 (general-purpose 에이전트) 에서 출시 차단 BLOCKER 2건 + SHOULD-FIX 1건 발견 → 즉시 hotfix
- **수행한 작업**:
  1. **Migration 050b** 작성 (`saas/migrations/050b-fix-trigger-security-and-outcome-ratio.sql`):
     - **(BLOCKER)** `fn_refresh_technique_stats` 에 `SECURITY DEFINER` + `SET search_path = public` 추가
       → 050 원본은 호출자 컨텍스트에서 실행되어 RLS (`ratings_select_own`) 가 적용,
         다른 사용자 ratings 가 SELECT 결과에서 제외됐음. 두 명 이상 평가 시 통계 silently 잘못됨.
         technique_stats 도 RLS 활성이라 INSERT 거부 가능성 있었음. 함수 소유자(super-user) 권한
         으로 RLS 우회.
     - **(SHOULD-FIX)** outcome 비율 분모를 `outcome IS NOT NULL` 인 행으로 한정
       → 별점만 보내고 outcome 비운 평가가 가중치를 0 으로 처지지 않도록.
         모든 outcome NULL 이면 분모/분자 0 → COALESCE 로 0.5 (중립) 처리.
  2. **api/feedback.js** 수정: ratings INSERT payload 에 `was_ai_recommended: true` 추가
     → 모든 평가가 AI 추천 카드 하단에서 발생하므로 항상 true. 050 의 `ai_recommended_count`
       메트릭이 영원히 0 이던 문제 해결.
- **결과물**:
  - `pt-prescription/saas/migrations/050b-fix-trigger-security-and-outcome-ratio.sql`
  - `pt-prescription/api/feedback.js` (1줄 추가)
- **이슈/메모**:
  - 050 의 ALTER TABLE / 트리거 등록 / 백필은 그대로 유지 — 050b 는 함수만 CREATE OR REPLACE
  - 대표님 액션: Supabase SQL 에디터에서 050b 실행 → 검증 쿼리로 `prosecdef = true` 확인

---

## 2026-05-03 | C-3 hotfix #2 — 049c ratings 누락 컬럼 보강

- **지시 받은 내용**: production 별점 저장 시 PostgREST 가 `acuity` 컬럼 미존재 400 에러 → /api/feedback 502 반환. 즉시 핫픽스.
- **수행한 작업**:
  1. error_logs 조회 → `"PGRST204": Could not find the 'acuity' column of 'ratings' in the schema cache"` 확인
  2. `information_schema.columns` 쿼리 → production ratings 가 schema.sql 정의 (21 컬럼) 와 정확히 일치, migration 041 의 region/acuity/symptom/technique_label/category_key/notes 6개 부재 확인
  3. 원인: PR #30 머지 시점에 schema.sql 이 적용됐거나 migration 041 이 production 에 안 깔린 상태로 운영돼 왔음
  4. **Migration 049c** 작성: `ALTER TABLE ratings ADD COLUMN IF NOT EXISTS` 6개 + `NOTIFY pgrst, 'reload schema'` (캐시 즉시 새로고침)
- **결과물**: `pt-prescription/saas/migrations/049c-ratings-missing-columns.sql`
- **이슈/메모**:
  - schema.sql 의 patient_profile / clinical_context / application_notes / response_notes 는 미래 확장용으로 유지 (제거 안 함)
  - 대표님 액션: Supabase SQL 에디터에서 049c 실행 → 별점 다시 저장 → 정상 확인
  - 별도 잠재 이슈: `ratings.technique_id NOT NULL` (schema.sql) — Anatomy Trains 등 techniques 행 없는 케이스 평가 시 NULL 허용으로 ALTER 필요할 수 있음. 즉시 출시 차단 아님.

---
