# sw-frontend-dev 월간 작업 로그 — 2026-05

> 작업할 때마다 append 방식으로 기록합니다. 삭제 금지.

---

## 2026-05-02 | todo-20260502-sw-frontend-verify 신규 부위 UI + 하이브리드 추천 UI 확인

- **지시 받은 내용**: Shoulder/Knee/Hip/Ankle 신규 기법 카드 UI 표시 확인, 하이브리드 추천 결과 UI 방향 결정
- **수행한 작업**:
  1. index.html:1725~1728 확인 — REGIONS 배열에 shoulder / knee / hip / ankle_foot 4개 부위 포함 확인
  2. 하이브리드 추천 결과 UI 검토 — 벡터 점수 UI 미노출로 결정, 내부 처리만
- **결과물**: sw-frontend-dev/TODO.md [x] 2개 체크 완료
- **이슈/메모**: 없음

---

## 2026-05-03 | C-3 — 별점 + 효과 평가 UI 확장

- **지시 받은 내용**: 인라인 피드백 섹션에 outcome (효과 분류) + indication_accuracy (적응증 정확도) UI 추가. 둘 다 선택사항으로 처리.
- **수행한 작업**:
  1. **CSS 추가** (`index.html` line 1399 직후): `.inline-fb-extra` 컨테이너, `.inline-fb-outcome` 라디오 그룹 (pill 스타일 6개), `.inline-fb-accuracy` 1~5 그리드 라디오 + 양 끝 "부적합/적합" 레이블
  2. **HTML 확장** (line ~3000): 별점 클릭 후 노출되는 영역에 outcome 라디오 6개 (excellent/good/moderate/poor/no_effect/adverse — 한국어 레이블) + accuracy 1~5 라디오 추가. 노트 textarea 위쪽에 배치.
  3. **JS 핸들러 확장** (`initFeedbackSection`): selectedOutcome / selectedAccuracy 상태 변수 추가, 라디오 토글 (재클릭 시 해제), 건너뛰기 시 두 필드 초기화, 저장 시 payload 에 outcome / indication_accuracy 포함, 저장 성공 시 두 영역 hide
- **결과물**: `pt-prescription/index.html` (CSS 약 50줄 + HTML 약 25줄 + JS 약 35줄 추가/수정)
- **이슈/메모**:
  - 두 필드 모두 선택사항 (null 허용). 별점만 5★ 클릭하고 outcome/accuracy 비워도 저장 가능.
  - 시각적 일관성: outcome 은 pill (가로 wrap), accuracy 는 5칸 grid (양 끝 텍스트 레이블)
  - submit handler 의 success path 에서 outcomeWrap/accuracyWrap 도 hide 처리 추가 누락 → 수정 완료
  - 백엔드 (api/feedback.js) 와 동시 반영 → 둘 다 머지돼야 동작

---

## 2026-05-04 | 베타 출시 D-1 — 첫-사용 환영 모달 추가

- **지시 받은 내용**: 베타 사용자 첫인상 개선 — 첫 로그인 시 사용 가이드 모달 노출 (정중·친절 톤)
- **수행한 작업**:
  1. CSS `.welcome-overlay` / `.welcome-modal` / `.welcome-step` 등 신규 (~85줄, fade-in 애니메이션 포함)
  2. HTML `welcomeOverlay` div: 4 step 카드 (📍 환자 조건 → 🎯 3박자 → 🤖 추천 8~15초 → ⭐ 별점·효과 평가) + 풋노트 (피드백 채널·한도 안내)
  3. JS: `WELCOME_KEY = 'pt-welcome-v1-dismissed'` localStorage 플래그, `showWelcomeModalIfFirstTime()` / `dismissWelcomeModal()` 함수, `goScreen2()` 진입 시 호출 (첫 로그인 1회만)
  4. localStorage 차단 환경 (사파리 시크릿 등) 은 try/catch 로 graceful 스킵
- **결과물**: `pt-prescription/index.html` (~150줄 추가)
- **이슈/메모**:
  - 모달 z-index 1100 (gfb 모달 1000 보다 위) — 동시 노출 시 welcome 우선
  - 사용자 피드백 한 줄: "베타 사용자가 8~15초 빈 화면 보고 의심하는 거 방지" 가 핵심 목적
  - 추후 (D+7~) 풀 인터랙티브 튜토리얼은 BACKLOG 추가 검토

---

## 2026-05-03 | C-3 cleanup — 사용 평가 모달 제거 (인라인으로 일원화)

- **지시 받은 내용**: 추천 카드에 "★ 사용 평가" (모달) 와 "이 추천이 임상에서 도움이 됐나요?" (인라인) 두 개 UI 가 공존 → 사용자 혼란 + 데이터 품질 격차 (모달 사용 시 outcome/accuracy 미수집, was_ai_recommended=false). 옵션 A (모달 제거) 진행.
- **수행한 작업**:
  1. **HTML**: `fbModalOverlay` div 전체 제거, 카드 렌더링의 `★ 사용 평가` 버튼 + `feedback-btn-wrap` 컨테이너 + `fbId` 변수 제거
  2. **CSS**: `.fb-modal*` / `.fb-context-chips` / `.fb-chip` / `.fb-star*` / `.fb-notes` / `.fb-save-btn` / `.btn-feedback*` / `.feedback-btn-wrap` 제거. `.fb-section-label` 만 유지 (gfb 모달이 재사용)
  3. **JS**: `openFeedback`, `closeFeedbackModal`, `setRating`, `updateStars`, `submitFeedback` 함수 + `fbCurrentRating`, `fbActiveBtnId`, `fbCurrentTech`, `fbCurrentCategory` 변수 제거 (~95줄)
  4. **인라인 섹션 dead code**: 모달 버튼 ✅ 표시하던 `.btn-feedback` 참조 제거 (모달 자체가 사라졌으니 무의미)
  5. **gfb (일반 피드백 — 헤더 💬 버튼)** 은 별도 기능이라 그대로 유지
- **결과물**: `pt-prescription/index.html` (267줄 deletion / 6줄 insertion)
- **이슈/메모**:
  - 베타 사용자가 인라인 피드백 한 진입점만 보게 됨 → 모든 평가가 outcome/accuracy/was_ai_recommended 풀 신호로 수집
  - 회귀: 인라인 섹션은 변경 없음, 모달 관련 코드만 제거 → 인라인 동작 그대로

---
