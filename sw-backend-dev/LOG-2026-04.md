# sw-backend-dev 월간 작업 로그 — 2026-04

> 이 파일은 작업할 때마다 **append** 방식으로 기록합니다. 삭제 금지.
> 매달 1일에 새 파일 `LOG-YYYY-MM.md`를 생성하세요. (예: 5월 1일 → LOG-2026-05.md)

---

## 2026-04-28 | todo-20260428-pgvector pgvector 시멘틱 검색 구현

- **지시 받은 내용**: Voyage AI + pgvector 기반 시멘틱 매칭 구현
- **수행한 작업**: 025-pgvector-setup.sql 마이그레이션, embed-techniques.js 임베딩 생성 스크립트, recommend.js hybrid scoring (ruleScore*0.6 + vectorScore*0.4) 구현
- **결과물**: pt-prescription/saas/migrations/025-pgvector-setup.sql, pt-prescription/saas/scripts/embed-techniques.js, pt-prescription/api/recommend.js (수정)
- **이슈/메모**: VOYAGE_API_KEY 환경변수 Vercel + 로컬 .env 설정 필요. embed-techniques.js는 SUPABASE_SERVICE_ROLE_KEY 필요 (RLS 우회). vector 없으면 룰 기반 단독 fallback 동작.

---

## 2026-04-28 | todo-20260428-embed-done 임베딩 배치 완료

- **지시 받은 내용**: 대표님이 embed-techniques.js 실행 후 완료 확인
- **수행한 작업**: technique_embeddings 테이블 180개 기법 임베딩 적재 완료 확인
- **결과물**: Supabase technique_embeddings 테이블 (180개 행)
- **이슈/메모**: 없음

---

## 2026-04-28 | todo-20260428-vector-id-fix recommend.js vectorScore 매핑 버그 픽스

- **지시 받은 내용**: 시멘틱 매칭 벡터 추천 실제 작동 여부 점검
- **수행한 작업**: fetchActiveTechniques selectFields에 id 누락 발견 → 추가 수정. t.id=undefined → vectorScoreMap 매핑 실패로 벡터 점수가 항상 0이었던 버그 수정
- **결과물**: pt-prescription/api/recommend.js (selectFields에 id 추가)
- **이슈/메모**: 수정 전: ruleScore만 작동(사실상 ruleNorm*0.6). 수정 후: ruleNorm*0.6 + vectorScore*0.4 정상 하이브리드

---

## 2026-04-28 | todo-20260428-d-neural-fix recommend.js d_neural 503 버그 픽스

- **지시 받은 내용**: 경추+신경가동술 선택 시 503 오류 발생 원인 조사 및 수정
- **수행한 작업**: fetchActiveTechniques가 DB 오류와 빈 결과(body_region 필터 후 empty)를 구분하지 못해 오인 503 발생 → null/[] 반환값 분리, 503 가드 조건 수정
- **결과물**: pt-prescription/api/recommend.js (fetchActiveTechniques null 반환, 503 가드 로직 개선)
- **이슈/메모**: 026-cervical-neural-techniques.sql Supabase 실행 후 경추 d_neural 실제 추천도 가능해짐

---

## 2026-04-29 | todo-20260429-502-retry 발목 502 오류 retry 로직 추가

- **지시 받은 내용**: 발목·아급성·움직임통증 502 AI 파싱 오류 해결
- **수행한 작업**: callLLMAndParse()에 1회 자동 재시도 로직 추가. ```json 블록 + 순수 JSON 두 패턴 모두 처리하도록 정규식 개선
- **결과물**: pt-prescription/api/recommend.js (callLLMAndParse 수정)
- **이슈/메모**: Claude Haiku가 간헐적으로 JSON 앞뒤에 마크다운 ```json 블록을 붙이는 경우가 원인. retryCount 파라미터로 최대 1회 재시도

---

## 2026-04-29 | todo-20260429-debug-api 벡터 점수 디버그 API 신규 생성

- **지시 받은 내용**: 벡터 점수가 실제로 작동하는지 확인할 수 있는 별도 API 제작
- **수행한 작업**: debug-scores.js 신규 API 엔드포인트 작성. ruleScore/vectorScore/finalScore 전체 계산 내역 반환. technique_embeddings 조회 후 기법 간 cosine similarity 0.6 이상 쌍 계산 (그래프 엣지용)
- **결과물**: pt-prescription/api/debug-scores.js (신규)
- **이슈/메모**: 인증 불필요 (대표님 전용 내부 디버그 도구). match_threshold=0.1로 낮춰 최대 50개 유사 기법 반환

---
