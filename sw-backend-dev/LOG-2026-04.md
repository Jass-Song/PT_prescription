# sw-backend-dev 월간 작업 로그 — 2026-04

> 이 파일은 작업할 때마다 **append** 방식으로 기록합니다. 삭제 금지.
> 매달 1일에 새 파일 `LOG-YYYY-MM.md`를 생성하세요. (예: 5월 1일 → LOG-2026-05.md)

---

## 2026-04-30 | todo-20260430-server-logging 서버 에러 로깅 헬퍼 추가

- **지시 받은 내용**: 서버 API 에러가 error_logs 테이블에 기록되지 않는 문제 수정
- **수행한 작업**: api/_logger.js 생성 (SUPABASE_SERVICE_KEY 사용, RLS 우회, fail-silent). feedback.js에 logServerError 2곳 추가 (technique lookup 실패, ratings INSERT 실패). recommend.js에 logServerError 5곳 추가 (Anthropic API 오류, JSON 추출 실패, JSON.parse 실패, LLM 호출 오류, Supabase fetch 오류, 후처리 오류)
- **결과물**: api/_logger.js, api/feedback.js, api/recommend.js
- **이슈/메모**: SERVICE_KEY 없으면 즉시 return (fail-silent). logServerError는 await 사용으로 응답 전 로깅 완료 보장

---

## 2026-04-28 | todo-20260428-pgvector pgvector 시멘틱 검색 구현

- **지시 받은 내용**: Voyage AI + pgvector 기반 시멘틱 매칭 구현
- **수행한 작업**: 025-pgvector-setup.sql 마이그레이션, embed-techniques.js 임베딩 생성 스크립트, recommend.js hybrid scoring (ruleScore*0.6 + vectorScore*0.4) 구현
- **결과물**: pt-prescription/saas/migrations/025-pgvector-setup.sql, pt-prescription/saas/scripts/embed-techniques.js, pt-prescription/api/recommend.js (수정)
- **이슈/메모**: VOYAGE_API_KEY 환경변수 Vercel + 로컬 .env 설정 필요. embed-techniques.js는 SUPABASE_SERVICE_ROLE_KEY 필요 (RLS 우회). vector 없으면 룰 기반 단독 fallback 동작.

---

## 2026-04-28 | todo-20260428-embed-done 임베딩 배치 완료

- **지시 받은 내용**: 대표님이 embed-techniques.js 실행 후 완료 확인
- **수행한 작업**: technique_embeddings 테이블 180개 기법 임베딩 적재 완료 확인
- **결과물**: Supabase technique_embeddings 테이블 (180개 행)
- **이슈/메모**: 없음

---

## 2026-04-28 | todo-20260428-vector-id-fix recommend.js vectorScore 매핑 버그 픽스

- **지시 받은 내용**: 시멘틱 매칭 벡터 추천 실제 작동 여부 점검
- **수행한 작업**: fetchActiveTechniques selectFields에 id 누락 발견 → 추가 수정. t.id=undefined → vectorScoreMap 매핑 실패로 벡터 점수가 항상 0이었던 버그 수정
- **결과물**: pt-prescription/api/recommend.js (selectFields에 id 추가)
- **이슈/메모**: 수정 전: ruleScore만 작동(사실상 ruleNorm*0.6). 수정 후: ruleNorm*0.6 + vectorScore*0.4 정상 하이브리드

---

## 2026-04-28 | todo-20260428-d-neural-fix recommend.js d_neural 503 버그 픽스

- **지시 받은 내용**: 경추+신경가동술 선택 시 503 오류 발생 원인 조사 및 수정
- **수행한 작업**: fetchActiveTechniques가 DB 오류와 빈 결과(body_region 필터 후 empty)를 구분하지 못해 오인 503 발생 → null/[] 반환값 분리, 503 가드 조건 수정
- **결과물**: pt-prescription/api/recommend.js (fetchActiveTechniques null 반환, 503 가드 로직 개선)
- **이슈/메모**: 026-cervical-neural-techniques.sql Supabase 실행 후 경추 d_neural 실제 추천도 가능해짐

---

## 2026-04-29 | todo-20260429-502-retry 발목 502 오류 retry 로직 추가

- **지시 받은 내용**: 발목·아급성·움직임통증 502 AI 파싱 오류 해결
- **수행한 작업**: callLLMAndParse()에 1회 자동 재시도 로직 추가. ```json 블록 + 순수 JSON 두 패턴 모두 처리하도록 정규식 개선
- **결과물**: pt-prescription/api/recommend.js (callLLMAndParse 수정)
- **이슈/메모**: Claude Haiku가 간헐적으로 JSON 앞뒤에 마크다운 ```json 블록을 붙이는 경우가 원인. retryCount 파라미터로 최대 1회 재시도

---

## 2026-04-29 | todo-20260429-body-regions body_regions 다중 부위 지원

- **지시 받은 내용**: NEU-SH-Median/Radial/Ulnar가 경추 방사통에도 적용되므로 shoulder+cervical 동시 부위 등록 필요
- **수행한 작업**: recommend.js + debug-scores.js selectFields에 body_regions 추가, 필터 로직을 body_regions 배열 우선(없으면 body_region fallback)으로 개선. isPrimary도 동일 로직 적용
- **결과물**: api/recommend.js, api/debug-scores.js (수정), saas/migrations/037-add-body-regions-array.sql (신규)
- **이슈/메모**: Supabase SQL Editor에서 037 실행 필수 (ALTER TABLE + UPDATE 3건)

---

## 2026-04-29 | todo-20260429-debug-api 벡터 점수 디버그 API 신규 생성

- **지시 받은 내용**: 벡터 점수가 실제로 작동하는지 확인할 수 있는 별도 API 제작
- **수행한 작업**: debug-scores.js 신규 API 엔드포인트 작성. ruleScore/vectorScore/finalScore 전체 계산 내역 반환. technique_embeddings 조회 후 기법 간 cosine similarity 0.6 이상 쌍 계산 (그래프 엣지용)
- **결과물**: pt-prescription/api/debug-scores.js (신규)
- **이슈/메모**: 인증 불필요 (대표님 전용 내부 디버그 도구). match_threshold=0.1로 낮춰 최대 50개 유사 기법 반환

---

## 2026-04-29 | todo-20260429-vector-fix 벡터 점수 0 복구

- **지시 받은 내용**: 연결선 업데이트 후 벡터 점수가 모두 0으로 사라진 문제 수정
- **수행한 작업**: 3가지 근본 원인 발견 및 수정 ① fetchTechniquePairSimilarities 컬럼명 오타 (technique_id→id) — 의미 연결선 항상 0의 원인 ② migration 040 작성 — technique_embeddings anon 읽기 정책 추가 + match_techniques SECURITY DEFINER 재생성 (Vercel anon키 RLS 차단 해결) ③ stats에 voyageKeySet 진단 필드 추가 (미설정 시 명시적 경고 표시)
- **결과물**: api/debug-scores.js (수정), debug/index.html (수정), saas/migrations/040-fix-embeddings-rls.sql (신규) — main 머지·배포 완료
- **이슈/메모**: VOYAGE_API_KEY가 Vercel 환경변수에 없으면 query embedding은 여전히 X. Vercel 대시보드에서 VOYAGE_API_KEY 등록 필요. Migration 040 Supabase SQL Editor 실행 필수

---

## 2026-04-30 | todo-20260430-5 usage_logs 자동 기록 + 9개 시나리오 검증

- **지시 받은 내용**: recommend.js usage_logs 기록 + 경추·요추 9개 시나리오 검증 매트릭스
- **수행한 작업**: logSessionUsage 헬퍼 추가(recommend.js 상단) + startTime 변수 추가 + Anatomy Trains 조기 반환 경로 및 메인 응답 경로 두 곳에 fire-and-forget 호출 삽입. migration 043-session-logs.sql 신규 생성(기존 usage_logs는 technique_id NOT NULL로 세션 단위 기록 불가 — 별도 session_logs 테이블 추가). SCENARIO-VERIFICATION.md 작성(9개 시나리오 매트릭스 + curl 명령어)
- **결과물**: api/recommend.js, saas/migrations/043-session-logs.sql, sw-backend-dev/SCENARIO-VERIFICATION.md
- **이슈/메모**: 기존 usage_logs 테이블은 technique_id NOT NULL 제약으로 세션 단위 기록 불가 → session_logs 별도 테이블. Supabase SQL Editor에서 043 실행 필요. SUPABASE_SERVICE_ROLE_KEY 환경변수 설정 시 RLS 우회하여 INSERT 가능.

---

## 2026-04-29 | todo-20260429-recommend-body-regions recommend.js body_regions 다중 부위 지원

- **지시 받은 내용**: NEU-SH-Median/Radial/Ulnar 등 shoulder+cervical 동시 적용 기법을 경추 검색에서도 표시
- **수행한 작업**: recommend.js fetchActiveTechniques selectFields에 body_regions 추가. body_regions 배열 우선(없으면 body_region fallback) 필터 로직 적용. isPrimary 계산도 body_regions 배열 기반으로 수정
- **결과물**: pt-prescription/api/recommend.js (수정) — feature/recommend-body-regions 브랜치 푸시 완료
- **이슈/메모**: Migration 037(body_regions 컬럼 추가) Supabase 실행 필요

---

## 2026-04-29 | todo-20260429-vector-fix-confirmed 벡터 점수 복구 완료 확인

- **지시 받은 내용**: Migration 040 실행 후 벡터 점수 정상 표시 여부 확인
- **수행한 작업**: Supabase SQL Editor에서 Migration 040 실행 완료(anon 읽기 정책 + SECURITY DEFINER). 대표님 확인: "잘 나오네요" — 벡터 점수 및 의미 연결선 정상 복구
- **결과물**: 없음 (확인 완료)
- **이슈/메모**: 없음

---
