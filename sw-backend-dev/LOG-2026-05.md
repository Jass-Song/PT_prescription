# sw-backend-dev 월간 작업 로그 — 2026-05

> 작업할 때마다 append 방식으로 기록합니다. 삭제 금지.

---

## 2026-05-01 | todo-20260501-embedding-qa 임베딩 현황 파악 + 쿼리 텍스트 품질 개선

- **지시 받은 내용**: 임베딩 현황 확인 + 쿼리 텍스트 개선으로 벡터 검색 품질 향상
- **수행한 작업**:
  1. Supabase 임베딩 현황 조회: 총 371개 기법 중 357개 임베딩 완료, 14개 미임베딩
  2. `api/recommend.js` 라인 822~823: 쿼리 텍스트를 단순 문자열 조합에서 임상적 문장 구조로 개선
     - 변경 전: `` `${region} ${acuity} ${symptom}` ``
     - 변경 후: `` `${region} ${acuityLabel} 환자. 주소증: ${symptom}. 수기치료 적응증 검색.` ``
     - acuity → acuityLabel 변환 추가 (급성→급성기, 아급성→아급성기, 만성→만성)
  3. `saas/scripts/embed-techniques.js` `buildEmbeddingText()` 함수 개선:
     - `symptom_tags`, `contraindication_tags` 배열 필드를 임베딩 텍스트에 추가
     - select 쿼리에 `symptom_tags`, `contraindication_tags` 필드 추가
- **결과물**: `api/recommend.js` (쿼리 텍스트 수정), `saas/scripts/embed-techniques.js` (임베딩 텍스트 + select 쿼리 수정)
- **이슈/메모**:
  - 미임베딩 14개 존재 → `node saas/scripts/embed-techniques.js --only-new` 실행 필요 (VOYAGE_API_KEY, SUPABASE_SERVICE_ROLE_KEY 환경 변수 필요)
  - 기존 357개 임베딩은 symptom_tags/contraindication_tags 미포함 상태 → 재임베딩 시 품질 향상 기대 (전체 재실행 필요 시 --only-new 없이 실행)
  - embedded_text 샘플 품질: technique_steps가 상세하여 기본 품질은 양호, 태그 추가로 더 풍부해짐

---

## 2026-05-02 | todo-20260502-sw-backend-verify 카테고리 다양성 강제 로직 확인

- **지시 받은 내용**: recommend.js maxPerCategory 파라미터 구현 및 동작 확인
- **수행한 작업**: recommend.js:598~630 maxPerCategory 파라미터 구현 확인 — "카테고리당 최대 1" 다양성 강제 동작 검증
- **결과물**: sw-backend-dev/TODO.md [x] 1개 체크 완료
- **이슈/메모**: 없음

---

## 2026-05-03 | C-3 — feedback.js API 확장 (outcome + indication_accuracy)

- **지시 받은 내용**: `api/feedback.js` 가 별점 외에도 outcome (효과 분류 ENUM) + indication_accuracy (1~5) 를 받아 ratings 테이블에 INSERT 하도록 확장.
- **수행한 작업**:
  1. **request body 파싱 확장**: `outcome`, `indication_accuracy` 두 필드 추가 (선택)
  2. **검증 추가**:
     - `outcome` ∈ `['excellent', 'good', 'moderate', 'poor', 'no_effect', 'adverse']` 또는 null/undefined
     - `indication_accuracy` ∈ 1~5 숫자 또는 null/undefined
     - 둘 다 미입력이면 NULL 로 INSERT (기존 동작 호환)
  3. **payload 확장**: ratings INSERT 시 두 컬럼 함께 전달 (Math.round 적용)
- **결과물**: `pt-prescription/api/feedback.js` (약 20줄 추가/수정)
- **이슈/메모**:
  - 두 컬럼은 migration 049 가 ratings 테이블에 추가하는 컬럼들. 마이그레이션 미적용 시 Supabase 가 unknown column 에러 반환 → 양쪽 동시 머지 필요.
  - 기존 별점만 보내는 구버전 클라이언트도 backward compatible (두 필드 NULL 처리)
  - C-4 트리거 (migration 050) 가 본 컬럼들로 가중치 계산 → end-to-end 검증은 Supabase 에서 verify-recommendation-weight-trigger.sql 실행
  - main 머지 후 충돌 해결: feedback.js 가 _logError → _logger 로 이동 + try/catch 단순화된 구조에 outcome/accuracy 추가 통합

---

## 2026-05-03 | C-4 hotfix — feedback.js 에 was_ai_recommended:true 추가

- **지시 받은 내용**: 정적 QA 리뷰에서 `was_ai_recommended` 필드 미전송 BLOCKER 발견 → 즉시 추가
- **수행한 작업**: `api/feedback.js` payload 에 `was_ai_recommended: true` 한 줄 추가. 모든 평가가 AI 추천 카드 하단에서 발생하므로 항상 true.
- **결과물**: `pt-prescription/api/feedback.js` (1줄 추가)
- **이슈/메모**: 050 트리거의 `ai_recommended_count` 가 항상 0 으로 집계되던 문제 해결. 050b 마이그레이션과 함께 hotfix PR 로 묶어 머지.

---

## 2026-05-03 | hotfix — recommend.js session_logs envvar 통일

- **지시 받은 내용**: Vercel 환경변수 점검 시 `recommend.js:65` 의 `SUPABASE_SERVICE_ROLE_KEY` 가 Vercel 의 `SUPABASE_SERVICE_KEY` 와 이름 불일치 발견 → session_logs INSERT 가 anon key 로 폴백되어 RLS 거부 → fire-and-forget 이라 silently 실패하던 상태. 즉시 수정.
- **수행한 작업**: `api/recommend.js:65` 의 변수명 `SUPABASE_SERVICE_ROLE_KEY` → `SUPABASE_SERVICE_KEY` 1줄 변경. 나머지 모든 `api/*.js` 와 명명 통일.
- **결과물**: `pt-prescription/api/recommend.js` (1줄 변경)
- **이슈/메모**:
  - 출시 차단은 아니었으나 분석용 데이터 손실 방지 목적
  - Notion "Outcome measure" §2 "세션당 추천 요청 수" 등 분석 쿼리가 정상 작동하려면 본 수정 필요
  - 로컬 스크립트 (saas/scripts/embed-techniques.js, backfill-applicable-muscles.js) 는 그대로 SUPABASE_SERVICE_ROLE_KEY 사용 — 대표님 .env 와 일치라 변경 불필요
