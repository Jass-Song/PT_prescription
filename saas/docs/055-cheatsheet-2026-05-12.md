# 055 Production 적용 — Cheat Sheet (한 페이지 TL;DR)

**작성일**: 2026-05-12 · **작성자**: sw-db-architect · **대상**: 대표님 SQL 에디터 옆 띄우기용
**풀 가이드**: `saas/docs/055-production-apply-guide-2026-05-12.md` (상세 — 비상 상황 시 참조)

> 핵심 3 원칙: ① **055a / 055b 같은 창 금지** (PG 55P04) · ② **각 블록 새 쿼리 창** · ③ **다운타임 0** (모두 멱등 + metadata-only).

---

## §1. 결정 트리

```
  [Block 1: pg_enum 카운트] ──► 7값 ──► Step 2A (Block 4~7 검증만)
                            ──► 6값 ──► Step 2B (Block 2 → Block 3 → Block 4~7)
                            ──► 그외 ─► Step 2D (중단, Jarvis 보고)

  Step 2A 중 Block 4~7 일부 FAIL ──► Step 2C (Block 3 단독 재실행 → Block 4~7)
```

---

## §2. SQL 블록 (복붙용 — 각 블록은 새 쿼리 창)

### Block 1 — pg_enum 카운트 (진단)
**언제**: Step 1, 가장 먼저. 6/7 분기 판단.
```sql
SELECT
  '1. rating_outcome ENUM 7 값 + not_used' AS check_label,
  CASE WHEN COUNT(*) >= 7
        AND bool_or(enumlabel = 'not_used')
        AND bool_or(enumlabel = 'excellent') AND bool_or(enumlabel = 'good')
        AND bool_or(enumlabel = 'moderate')  AND bool_or(enumlabel = 'poor')
        AND bool_or(enumlabel = 'no_effect') AND bool_or(enumlabel = 'adverse')
       THEN 'PASS' ELSE 'FAIL' END AS result,
  COUNT(*) AS value_count,
  array_agg(enumlabel ORDER BY enumsortorder) AS enum_values
FROM pg_enum WHERE enumtypid = 'rating_outcome'::regtype;
```
**예상**: `value_count = 7` PASS → Step 2A / `= 6` FAIL → Step 2B / 그 외 → Step 2D.

### Block 2 — 055a (ENUM 'not_used' 추가)
**언제**: Step 2B 첫 번째. **Block 3 와 같은 창 절대 금지** (55P04). 실행 후 새 창 열기.
```sql
DO $$ BEGIN
  ALTER TYPE rating_outcome ADD VALUE IF NOT EXISTS 'not_used';
EXCEPTION WHEN duplicate_object THEN null; END $$;

COMMENT ON TYPE rating_outcome IS
  '055 단일 신호 모델 (7 분류): excellent / good / moderate / poor / no_effect / adverse / not_used. not_used 는 PT 가 추천을 받았으나 미시술. weight 산식에서 effectiveness 분모/분자 제외 (영향 0). 활성도 카운트에는 포함.';

DO $$
DECLARE v_count INT;
BEGIN
  SELECT COUNT(*) INTO v_count FROM pg_enum WHERE enumtypid = 'rating_outcome'::regtype;
  RAISE NOTICE 'rating_outcome enum value count: % (기대: 7)', v_count;
END $$;
```
**예상**: Messages 탭 `NOTICE: rating_outcome enum value count: 7 (기대: 7)`.

### Block 3 — 055b (컬럼 + 함수 + 일괄 재계산)
**언제**: Step 2B 두 번째 (Block 2 commit 후 **새 쿼리 창**), 또는 Step 2C (단독 재실행).
**원본 복사**: `saas/migrations/055b-outcome-not-used-stats.sql` 파일 전체 (196줄).
- 핵심 변경: `not_used_count` 컬럼 추가 / `fn_refresh_technique_stats` 재정의 (`outcome != 'not_used'` 필터) / 일괄 `UPDATE technique_stats` 재계산.

**예상**: `ALTER TABLE / CREATE FUNCTION / UPDATE N` 정상 완료 (N = technique_stats 행 수).

### Block 4 — verify §2 함수 본문 검증
**언제**: Step 2A / 2B / 2C 검증 단계.
```sql
SELECT
  '2. fn_refresh_technique_stats — 055 본문 패턴' AS check_label,
  CASE WHEN function_count = 1 AND security_definer = true
        AND has_not_used_filter AND has_not_used_count
        AND has_070 AND has_020 AND has_010
       THEN 'PASS' ELSE 'FAIL' END AS result,
  function_count, security_definer, has_not_used_filter, has_not_used_count, has_070, has_020, has_010
FROM (
  SELECT COUNT(*) AS function_count,
    bool_and(prosecdef = true) AS security_definer,
    bool_or(pg_get_functiondef(oid) LIKE '%outcome != ''not_used''%') AS has_not_used_filter,
    bool_or(pg_get_functiondef(oid) LIKE '%not_used_count%') AS has_not_used_count,
    bool_or(pg_get_functiondef(oid) LIKE '%0.70%') AS has_070,
    bool_or(pg_get_functiondef(oid) LIKE '%0.20%') AS has_020,
    bool_or(pg_get_functiondef(oid) LIKE '%0.10%') AS has_010
  FROM pg_proc WHERE proname = 'fn_refresh_technique_stats'
) sub;
```
**예상**: PASS + 모든 플래그 `t`.

### Block 5 — verify §3 컬럼 존재 검증
```sql
SELECT
  '3. technique_stats.not_used_count 컬럼' AS check_label,
  CASE WHEN data_type = 'integer' AND column_default LIKE '%0%'
       THEN 'PASS' ELSE 'FAIL' END AS result,
  data_type, is_nullable, column_default
FROM information_schema.columns
WHERE table_schema = 'public' AND table_name = 'technique_stats' AND column_name = 'not_used_count';
```
**예상**: PASS / `integer` / DEFAULT 0.

### Block 6 — verify §4 weight 범위 검증
```sql
SELECT
  '4. recommendation_weight 범위 [0,1]' AS check_label,
  CASE WHEN COUNT(*) FILTER (
         WHERE recommendation_weight IS NOT NULL
           AND (recommendation_weight < 0.0 OR recommendation_weight > 1.0)) = 0
       THEN 'PASS' ELSE 'FAIL' END AS result,
  COUNT(*) AS total_rows,
  COUNT(*) FILTER (WHERE recommendation_weight IS NULL) AS null_count,
  MIN(recommendation_weight) AS min_weight, MAX(recommendation_weight) AS max_weight
FROM technique_stats;
```
**예상**: PASS / 위반 0.

### Block 7 — verify §5+§6 E2E (not_used + good INSERT, 자동 롤백)
**언제**: 마지막 검증. **DO 블록은 단독 실행 권장** (NOTICE 메시지 가독성). 원본: `saas/scripts/verify-055.sql` §5 / §6.
**예상**: Messages 탭에 `5. E2E not_used: PASS — ...` / `6. E2E good: PASS — ...` 양쪽 + 각 트랜잭션 롤백 완료 NOTICE.

---

## §3. 비상 상황 (1~2줄 처치)

- **`ERROR 55P04 unsafe use of new enum value`**: Block 2 와 Block 3 를 같은 창에서 실행함. **새 쿼리 창**에서 Block 3 만 다시 실행 (Block 2 ENUM 추가는 보존됨).
- **Block 4~7 중 FAIL**: Step 2C → Block 3 (055b) 단독 새 쿼리 창에서 재실행 → Block 4~7 재검증. 그래도 FAIL 시 Jarvis 보고.
- **`SKIP — 데이터 부족` 메시지** (Block 7): production 에서 거의 없음. `auth.users` / `techniques` 가 비었을 때만 발생 → Jarvis 보고.
- **`ERROR: type "rating_outcome" does not exist`**: 마이그 054 미적용. 진행 중단 → Jarvis 보고.
- **Block 1 결과가 5 / 8+ 또는 쿼리 자체 에러**: Step 2D — 스크린샷 + 에러 전문 캡처 후 Jarvis 보고. **무리 실행 금지**.

---

## §4. 적용 후 보고 양식 (Jarvis 회신용)

```
- 적용 일시: YYYY-MM-DD HH:MM
- Block 1: value_count = X (PASS/FAIL)
- 경로: Step 2A / 2B / 2C / 2D
- Block 4 §2: PASS/FAIL (FAIL 시 플래그)
- Block 5 §3: PASS/FAIL
- Block 6 §4: PASS/FAIL (FAIL 시 min/max)
- Block 7 §5: PASS/FAIL (NOTICE 본문)
- Block 7 §6: PASS/FAIL (NOTICE 본문)
- 이슈/관찰: (있다면)
```

---

## §5. 풀 가이드 Cross-Reference (상세 디버깅 시)

| 본 cheat sheet | 풀 가이드 (`055-production-apply-guide-2026-05-12.md`) |
|---|---|
| §1 결정 트리 | §0 결정 트리 + 핵심 원칙 3가지 |
| Block 1 | Step 1 (pg_enum 카운트) |
| Block 2 | Step 2B.1 (055a) + 운영 영향/롤백 |
| Block 3 | Step 2B.2 (055b 전체 SQL 196줄) + 운영 영향/롤백 |
| Block 4~7 | Step 2A.1 ~ 2A.5 (각 FAIL 시나리오 상세) |
| §3 비상 상황 | §3 FAQ Q1~Q6 |
| §4 보고 양식 | §4 적용 후 보고 양식 |

원본 SQL 우선 — 본 cheat sheet 와 차이 발견 시 `saas/migrations/055a*`, `055b*`, `saas/scripts/verify-055.sql` 이 기준.

---

**문서 끝.**
